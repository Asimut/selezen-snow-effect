/*!
 * Snow Effect by Selezen v1.0.2
 * Created by Selezen, © 2024
 */
(function() {
    'use strict';

    // Перевіряємо наявність jQuery
    function waitForJQuery(callback) {
        if (window.jQuery) {
            console.log('jQuery знайдено');
            callback(window.jQuery);
        } else {
            console.log('Очікування jQuery...');
            var script = document.createElement('script');
            script.src = 'https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js';
            script.onload = function() {
                console.log('jQuery завантажено');
                callback(window.jQuery);
            };
            document.head.appendChild(script);
        }
    }

    // Головна функція ініціалізації снігу
    function initSnowEffect(jQuery) {
        var $ = jQuery;
        var isRunning = false; // Флаг для відстеження стану ефекту

        // Лічильник активних сніжинок
        let activeSnowflakes = 0;
        const MAX_SNOWFLAKES = 5;

        // Налаштування за замовчуванням
        const defaultSettings = {
            character: "❄",
            speed: 40000,
            frequency: 250,
            color: "#F00000",
            blur: false,
            enabled: true,
            small: 2,
            large: 4,
            wind: 2,
            windVariance: 1,
            startOpacity: 0.2,
            endOpacity: 0,
            overflow: "hidden",
            zIndex: 9999
        };

        window.SnowEffect = {
            settings: {...defaultSettings},
            
            init: async function(scriptUrl) {
                try {
                    const response = await fetch(scriptUrl);
                    const data = await response.json();
                    Object.assign(this.settings, data);
                    
                    if (this.settings.enabled && !isRunning) {
                        console.log('Запуск ефекту снігу');
                        this.start();
                    } else if (!this.settings.enabled && isRunning) {
                        console.log('Зупинка ефекту снігу');
                        this.stop();
                    }
                } catch (error) {
                    console.error('Помилка завантаження налаштувань:', error);
                }
            },

            start: function() {
                if (isRunning) return; // Перевіряємо, чи не запущений вже ефект
                isRunning = true;

                const $container = $('<div>')
                    .addClass('snow-container')
                    .css({
                        position: 'fixed',
                        top: 0,
                        left: 0,
                        right: 0,
                        bottom: 0,
                        pointerEvents: 'none',
                        overflow: this.settings.overflow,
                        zIndex: this.settings.zIndex
                    })
                    .appendTo('body');

                const createFlake = () => {
                    if (!this.settings.enabled || activeSnowflakes >= MAX_SNOWFLAKES) return;

                    activeSnowflakes++;
                    const fontSize = Math.random() * (this.settings.large - this.settings.small) + this.settings.small;
                    const startX = Math.random() * window.innerWidth;
                    const endX = startX + Math.random() * this.settings.wind * 2 - this.settings.wind;
                    const endY = window.innerHeight;
                    const speed = this.settings.speed;

                    const $flake = $('<span>')
                        .html(this.settings.character)
                        .css({
                            position: 'absolute',
                            top: -fontSize,
                            left: startX,
                            color: this.settings.color,
                            fontSize: fontSize + 'px',
                            opacity: this.settings.startOpacity,
                            transition: `all ${speed/1000}s linear`,
                            transform: 'rotate(0deg)'
                        })
                        .appendTo($container);

                    requestAnimationFrame(() => {
                        $flake.css({
                            transform: `translate(${endX - startX}px, ${endY}px)`,
                            opacity: 0
                        });
                    });

                    setTimeout(() => {
                        activeSnowflakes--;
                        $flake.remove();
                    }, speed);
                };

                function generateFlakes() {
                    if (isRunning && window.SnowEffect.settings.enabled) {
                        createFlake();
                        setTimeout(generateFlakes, window.SnowEffect.settings.frequency);
                    }
                }

                generateFlakes();
            },

            stop: function() {
                isRunning = false;
                $('.snow-container').remove();
                activeSnowflakes = 0;
            }
        };

        // Запускаємо ефект
        window.SnowEffect.init('https://script.google.com/macros/s/AKfycbxXDc1zV88D0nJxYtc3KoPn6WTiG9qIo2zlo9RSccHi7cpEMMFireralBh3q6p-5uVDLA/exec');

        // Перевіряємо оновлення налаштувань кожні 5 секунд
        setInterval(function() {
            if (document.visibilityState === 'visible') {
                window.SnowEffect.init('https://script.google.com/macros/s/AKfycbxXDc1zV88D0nJxYtc3KoPn6WTiG9qIo2zlo9RSccHi7cpEMMFireralBh3q6p-5uVDLA/exec');
            }
        }, 5000);
    }

    // Запускаємо після завантаження сторінки
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', function() {
            waitForJQuery(initSnowEffect);
        });
    } else {
        waitForJQuery(initSnowEffect);
    }
})();