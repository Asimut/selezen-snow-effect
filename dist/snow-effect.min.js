/*!
 * Snow Effect by Selezen v1.0.4
 * Created by Selezen, © 2024
 */
(function() {
    'use strict';

    function waitForJQuery(callback) {
        if (window.jQuery) {
            console.log('jQuery знайдено');
            callback(window.jQuery);
        } else {
            console.log('Очікування jQuery...');
            var script = document.createElement('script');
            script.src = 'https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js';
            script.onload = function() {
                console.log('jQuery завантажено');
                callback(window.jQuery);
            };
            document.head.appendChild(script);
        }
    }

    function initSnowEffect(jQuery) {
        var $ = jQuery;
        var isRunning = false;
        let activeSnowflakes = 0;
        const MAX_SNOWFLAKES = 50;

        const defaultSettings = {
            character: "❄",
            speed: 10000,
            frequency: 100,
            color: "#FFFFFF",
            blur: false,
            enabled: true,
            small: 8,
            large: 16,
            wind: 100,
            windVariance: 50,
            startOpacity: 1,
            endOpacity: 0,
            overflow: "hidden",
            zIndex: 9999
        };

        window.SnowEffect = {
            settings: {...defaultSettings},
            
            init: async function(scriptUrl) {
                try {
                    if (scriptUrl) {
                        console.log('Завантаження налаштувань з Google Таблиці...');
                        const response = await fetch(scriptUrl);
                        if (!response.ok) {
                            throw new Error('Помилка завантаження налаштувань');
                        }
                        const data = await response.json();
                        
                        // Проверяем каждое свойство перед обновлением
                        Object.keys(defaultSettings).forEach(key => {
                            if (data[key] !== undefined && data[key] !== null) {
                                this.settings[key] = data[key];
                            }
                        });
                        
                        console.log('Налаштування оновлено:', this.settings);
                    }
                } catch (error) {
                    console.warn('Використовуються налаштування за замовчуванням:', error);
                }

                if (this.settings.enabled) {
                    if (!isRunning) {
                        console.log('Запуск ефекту снігу');
                        this.start();
                    }
                } else if (!this.settings.enabled && isRunning) {
                    console.log('Зупинка ефекту снігу');
                    this.stop();
                }
            },

            start: function() {
                if (isRunning) return;
                isRunning = true;

                const $container = $('<div>')
                    .addClass('snow-container')
                    .css({
                        position: 'fixed',
                        top: 0,
                        left: 0,
                        right: 0,
                        bottom: 0,
                        pointerEvents: 'none',
                        overflow: this.settings.overflow,
                        zIndex: this.settings.zIndex
                    })
                    .appendTo('body');

                const createFlake = () => {
                    if (!isRunning || activeSnowflakes >= MAX_SNOWFLAKES) return;

                    activeSnowflakes++;
                    const fontSize = Math.random() * (this.settings.large - this.settings.small) + this.settings.small;
                    const startX = Math.random() * window.innerWidth;
                    const windOffset = (Math.random() - 0.5) * this.settings.wind;
                    const endX = startX + windOffset;
                    const endY = window.innerHeight + fontSize;
                    
                    const $flake = $('<span>')
                        .html(this.settings.character)
                        .css({
                            position: 'absolute',
                            top: -fontSize,
                            left: startX,
                            color: this.settings.color,
                            fontSize: fontSize + 'px',
                            opacity: this.settings.startOpacity,
                            textShadow: '0 0 5px rgba(255,255,255,0.8)',
                            transition: `all ${this.settings.speed/1000}s linear`
                        })
                        .appendTo($container);

                    requestAnimationFrame(() => {
                        $flake.css({
                            transform: `translate(${windOffset}px, ${endY}px) rotate(360deg)`,
                            opacity: this.settings.endOpacity
                        });
                    });

                    setTimeout(() => {
                        activeSnowflakes--;
                        $flake.remove();
                    }, this.settings.speed);
                };

                const generateFlakes = () => {
                    if (isRunning) {
                        createFlake();
                        setTimeout(generateFlakes, this.settings.frequency);
                    }
                };

                generateFlakes();
            },

            stop: function() {
                isRunning = false;
                $('.snow-container').remove();
                activeSnowflakes = 0;
            },

            updateSettings: function(newSettings) {
                Object.assign(this.settings, newSettings);
                if (isRunning) {
                    this.stop();
                    this.start();
                }
            }
        };

        // Запускаем эффект и настраиваем периодическое обновление
        const GOOGLE_SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbxXDc1zV88D0nJxYtc3KoPn6WTiG9qIo2zlo9RSccHi7cpEMMFireralBh3q6p-5uVDLA/exec';
        
        window.SnowEffect.init(GOOGLE_SCRIPT_URL);

        // Проверяем обновления настроек каждые 5 секунд
        setInterval(function() {
            if (document.visibilityState === 'visible') {
                window.SnowEffect.init(GOOGLE_SCRIPT_URL);
            }
        }, 5000);
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', function() {
            waitForJQuery(initSnowEffect);
        });
    } else {
        waitForJQuery(initSnowEffect);
    }
})();